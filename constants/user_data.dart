class UserData {
  String? currentGender;
  double smokedWeed;
  double startedSmokingAge;
  int currentHeight;
  int currentWeight;
  int currentAge;

  UserData(
      {required this.currentGender,
      required this.smokedWeed,
      required this.startedSmokingAge,
      required this.currentHeight,
      required this.currentWeight,
      required this.currentAge,
      });
}
